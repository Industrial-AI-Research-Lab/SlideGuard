[tool.poetry]
name = "slideguard"
version = "0.1.0"
description = "Library for students slide decks evaluation"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11,<3.14"
crewai = "^0.150.0"
pydantic = "^2.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"